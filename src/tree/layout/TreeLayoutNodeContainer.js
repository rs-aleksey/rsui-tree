import styled from 'styled-components';

const TreeLayoutNodeContainer = styled.div`
  display: inline-flex;
  flex: 1;
  flex-direction: column;
  min-width: 100px;
  justify-content: center;
`;

TreeLayoutNodeContainer.displayName = 'TreeLayoutNodeContainer';

export default TreeLayoutNodeContainer;
